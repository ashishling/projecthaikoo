import {
  AlertTriangle,
  CheckCircle,
  Loader2,
  LucideProps,
} from "lucide-react"

export type Icon = LucideProps

export const Icons = {
  spinner: Loader2,
  alertTriangle: AlertTriangle,
  checkCircle: CheckCircle,
} 